import React from 'react';
import ProgressBar from '../../components/progress-bar';

const Home: React.FC = () => {
  return (
    <div>
      <ProgressBar />
    </div>
  );
};

export default Home;
